# Source resume artifacts — PII (phone number); ingested via docs/upload_report_*.md
input/

# Secrets & local env
.env
*.env
!.env.example
secrets/

# Generated graph + exports — derived from the vault, never edited (CLAUDE.md rule 1)
dist/
# Intermediate exporter staging (build/layers/) — disposable, never published
build/

# Assembled Pages tree (`make pages`) and the private overlay checkout CI reads
# it from. Both can hold tailored, employer-named CVs — committing either would
# put private content in this public repo's history permanently.
pages/
.apps/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/


# Terraform
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/*.tfvars
!infra/*.tfvars.example

# Claude Code local settings (personal; the project .claude/CLAUDE.md IS tracked)
.claude/settings.local.json
.claude/CLAUDE.local.md
next-prompts.md

