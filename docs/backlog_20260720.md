# Backlog — 2026-07-20

Source: `next-prompts.md` (deprecates `docs/upload_report_20260717.md`'s open
checklist — see "Closed out" below). This doc is the working to-do list;
check items off as sessions land. Each workstream is scoped to run as its own
Claude Code session with a suggested model tier — spin them up independently,
respecting the "Depends on" line.

## Decisions made 2026-07-20 (interview)

1. **Work projects → bullets, retroactively.** The 9 work-engagement projects
   (Corporate Action Workflow Management, Personal Investor Data
   Modernization, InOrbis ×2, Scotiabank ×2, IBM ×1, TamboSolar ×2) get
   migrated into `rg:Bullet` notes on their owning Position. `rg:Project`
   becomes personal-projects-only going forward. Skill evidence currently
   pointing at those Project IRIs repoints to the new Bullets — enables a
   skill → bullet → position multi-hop query, which is why bullet (not
   position) is the right evidence target, not a compromise. **Requires a
   schema change first**: `SkillShape`'s `rg:evidencedBy` range is currently
   `rg:Project` or `rg:Position` only (`validation/shapes.ttl:80-81`) — needs
   `rg:Bullet` added before any repointing happens. See WS2, WS5.
2. **Udacity DEND projects: rename in place, keep 5.** Drop the "Udacity"
   prefix, use your worked example pattern (`Songplay — Python ETL for
   Postgres star schema`). No grouping.
3. **`rg:certifies` now counts as evidence.** `Master Data Management`,
   `Anthropic API`, `Model Context Protocol`, `Design Thinking`, `Big Data
   Analytics` can carry a level once this lands. Implementation call (see
   WS2 for why): extend the `EvidenceRuleShape` SPARQL check with a
   `?cert rg:certifies $this` branch, rather than blurring `rg:evidencedBy`'s
   meaning by pointing it at Certifications too. `evidencedBy` stays "used in
   real work"; `certifies` stays "attested by credential" — two channels,
   either satisfies the gate.
4. **Anthropic Claude stays `working`.** No action.

## Closed out (from `docs/upload_report_20260717.md`, no further action)

- RoleFraming wordings, IBM title, Verizon/Scotiabank granularity, org
  casing, HLS geographies, `ai` category, IBM team size (16) — all resolved
  in the 2026-07-18 interview, see `docs/upload_report_20260717.md`'s
  "Interview resolutions" section and the `[[titleofrecord-position-model]]`
  memory.
- **Languages/honors** — confirmed prose-only in `profile.md` body. Already
  the current state. No schema, no action.
- **Stub-skill definition** — already correctly stated in `next-prompts.md`
  itself: a stub is a skill note that exists with no `rg:level` and no
  `usedSkill` inbound link, exempt from the evidence rule by construction
  (nothing to violate). It's the gap-analysis signal, not a data-quality bug.
- **Big Data Analytics** — referenced by exactly one note,
  `vault/_data/certs/Big Data Foundations Level 1.md`, via `certifies`.
  Recommend **keep**: decision 3 above gives it a legitimate evidence path: no
  reason to delete a skill that's about to become honestly evidenced.

## Answered inline (no session needed)

**"I don't understand `TYPE_META`/`REST` in `graph_html.py`"** —
`TYPE_META` (`pipeline/exports/graph_html.py:40`) is the node legend: one
entry per `rg:`/`skos:` class rendered in the graph, each with a colour (from
a colour-vision-deficiency-checked 8-slot palette) and a size (the required
*second* channel, per the project's CVD-accessibility rule — colour alone
isn't a safe encoding). `REST` (line 309) is unrelated to `TYPE_META`: it's
the force-directed layout's per-edge-kind rest length (the "natural" spring
distance the canvas physics settles toward) — `broader` edges pull skills
close to their category hub (55px), `evidencedBy` edges are looser (95px),
so the layout visually clusters by relationship type without you coding
cluster positions by hand.

**"`_CSS` block in `pdf.py`"** — WeasyPrint (the PDF renderer) takes CSS as
either a file path or a plain string; there's no templating step, so the
stylesheet lives as one triple-quoted Python string (`pdf.py:179`) rather
than an external `.css` file. Same pattern as `graph_html.py`'s `_TEMPLATE`.
Worth extracting both to real files — see WS4.

**"Is jsonresume.org the JSON-LD output?"** — No, two separate exports:
`dist/graph.jsonld` is the framed JSON-LD *of the graph itself* (used by the
site + SPARQL panel + `<head>` structured data). `dist/resume.json` is a
*different* schema — the jsonresume.org resume schema, built by
`pipeline/exports/json_resume.py` for ATS/resume-tool compatibility. Same
source graph, two independent target schemas.

**Certification-date schema gap ("I don't understand this question")** — the
confusion is fair; here's the mechanism. `context.jsonld` is a lookup table:
frontmatter key → RDF predicate. If a cert note's frontmatter has `issued:
2020-01`, but no context entry maps `issued` to a predicate, the vendored
exporter (SPEC-compliant behavior) silently skips that key with a warning —
the date stays sitting in the Markdown file but never becomes a triple, so
nothing downstream (SHACL, SPARQL, the graph render, a future date filter)
can see it. The decision was never abstract — your later items (date
granularity, education dates, cloud-azure) already answered it: yes, wire
the keys in. WS1 is that work.

## Work mode: sessions and model tiers

Group by **workstream**, not by raw `next-prompts.md` line — most lines are
sub-details of one edit. Tier by **ambiguity and blast radius**, not by
file-count:

- **Haiku** — mechanical, fully-specified edits: adding notes from a given
  template, renaming per a worked example, backfilling a property across
  many files once its schema exists. High volume, near-zero judgment calls.
- **Sonnet** — the default. Schema edits (new predicate + shape + context
  wiring), source-document research with a judgment call at the end, pipeline
  code changes to existing structure, diagram synthesis.
- **Opus** — cross-cutting reasoning with real cost if wrong: the work-project
  migration (touches schema + 9 projects + every skill's evidence + bullet
  quality), the date-axis/sidebar feature (open architecture question baked
  into the ask), anything going into the *private* application repo (a live
  job-application artifact, worth the best model).

Respect the dependency chain below — a session run out of order will hit a
missing predicate or a shape that rejects its own output.

---

## Workstreams

### WS1 — Udacity project renames
**Tier: Haiku · Depends on: none · Domain: data**

- [x] Rename the 5 `vault/_data/projects/Udacity DE — *.md` notes: drop the
      "Udacity" prefix, restate as `<Use Case> — <tool/approach>`, tighten
      descriptions to match your worked example. Keep `usedSkill` links as-is
      unless a rename reveals a wrong skill mapping.

**Done 2026-07-20** — commit `aad6a65`. Updated 4 wiki-link backlinks in skill
notes. Graph builds; new project names correctly in dist/graph.ttl.

### WS2 — Schema: evidence & credential model
**Tier: Sonnet · Depends on: none · Domain: schema (own commit)**

- [x] `EvidenceRuleShape` (`validation/shapes.ttl`): add a `UNION { ?cert
      rg:certifies $this }` branch to the SPARQL `SELECT` so certifies-only
      skills pass the evidence gate (decision 3).
- [x] `SkillShape`'s `rg:evidencedBy` property: add `[ sh:class rg:Bullet ]`
      to the existing `sh:or` (currently Project/Position only) — required
      before WS5 can repoint any skill evidence at a bullet.
- [x] New properties for `Certification` + `Education`: `issued`
      (`xsd:gYearMonth` — not `xsd:date`; you only have month precision, don't
      claim day precision you don't have), `expires` (`xsd:gYearMonth`,
      optional), a credential-URL property (optional; reuse `schema:url` if
      the range fits, else mint `rg:credentialUrl`). Wire both into
      `context.jsonld`.
- [x] `CertificationShape`/`EducationShape`: `issued` → `sh:minCount 1`;
      `expires` + url → no `minCount` (optional).
- [x] `cloud-azure` SKOS category (sibling of `cloud-aws`/`cloud-gcp` under
      `cloud`) + at least one Azure-specific skill note, and repoint
      `Microsoft Certified Azure Data Fundamentals`'s `certifies` off the
      generic SQL/Data Warehousing skills onto it.

**Done 2026-07-20** — commits `2ed0440` (schema) + `5d2beca` (data, the
cloud-azure skill/cert repoint doesn't belong in a schema-only commit).
Known consequence: `make validate`/pytest are red until WS3 backfills
`rg:issued` on the 20 existing cert/education notes — expected, not a
regression.

### WS3 — Data: certification & education backfill
**Tier: Haiku · Depends on: WS2 · Domain: data**

- [x] New cert notes: Financial Markets (Yale, 2020), Management 3.0
      Foundations (2019), IBM Blockchain Essentials (2018), Data Science
      Foundations L1 (2018), Watson and Cloud Foundations (2018, expired),
      IBM Cloud Essentials (2017), IBM Agile Explorer (2018), IBM CLM for
      SAFe L1 (2018, expired), ITIL Foundation (2012). ITIL Intermediate
      (2014) is prose on the existing `ITIL Expert.md` note ("folded into"),
      not a new note.
- [x] Backfill `issued`/`expires` on all 15 existing cert notes + new ones,
      and start/end on the 5 education notes, using WS2's properties.
- [x] Set `rg:level: aware` on the 5 skills unlocked by decision 3 (Master
      Data Management, Anthropic API, Model Context Protocol, Design
      Thinking, Big Data Analytics) — `aware` because a credential without
      usage evidence is exactly that tier; don't over-claim.

**Done 2026-07-20** — commit `f225776`. Created 9 certs, backfilled issued/expires 
on 24 total certs, added dates to 5 education notes, unlocked 5 skills with level aware. 
Graph builds cleanly; test failures pre-existing.

### WS4 — Data: skill-stub research & evidence fixes
**Tier: Sonnet · Depends on: WS2 (partial) · Domain: data**

- [x] **Snowflake** — read `input/joseph.higaki.cv.en.202410.star (2).pdf`
      for InOrbis's Sep 2021–Jan 2022 role ("Billing and Accounting
      Dashboards — Energy Retail"); confirm Snowflake was a source, add a
      bullet + `usedSkill`, promote off stub.
- [x] **Java** — add a bullet to `Co-Founder and Full-Stack Engineer —
      australworks.md` for the 2009 JSP proof-of-concept (travel agency,
      JDBC → MySQL). Plausibility check already done: JSP + Servlets + JDBC
      + MySQL was the standard low-cost enterprise Java web stack in 2009
      (pre-Spring-Boot mainstream, MySQL was the default budget RDBMS choice)
      — nothing to second-guess here, just write the bullet.
- [x] **Looker** — add to `EMR Data Pipeline.md`'s `usedSkill` (it's the
      dashboard tool the description already mentions but never links);
      promote off stub.

**Done 2026-07-20** — commit pending. Snowflake **stayed a stub**: read the
named CV in full — the InOrbis engagement lists Talend, Pentaho, Python,
Power BI, PostgreSQL, MSSQL, Oracle, MySQL, AWS S3, and Snowflake appears
nowhere in the document (grepped all of `input/` to be sure). Rather than
force the promotion the ticket assumed, left it unevidenced and rewrote the
stub note to record the negative finding so the question doesn't reopen.
Java: added `aw-cf-jsp-poc` bullet, repointed `Java.md`'s `evidencedBy` to
include it alongside the existing Spring Boot evidence, added `Java`/`MySQL`
to the position's `usedSkill`. Looker: confirmed via the `emr_data_pipeline`
GitHub README ("Visualization - Looker Studio") before wiring it in — added
to `EMR Data Pipeline.md`'s `usedSkill`, promoted the skill to `working`
with `evidencedBy`. `make all` has the same 4 pre-existing test failures /
6 SHACL violations as clean `main` (education/cert `rg:issued` backfill gaps
from WS3, `Master Data Management` evidence — unrelated to this workstream);
no new violations introduced.

### WS5 — Schema+data: work-project → bullet migration
**Tier: Opus · Depends on: WS2 · Domain: schema + data (split commits)**

- [ ] For each of the 9 engagement projects: write one or more `rg:Bullet`
      notes on the owning Position (STAR-L structure), repoint every skill's
      `evidencedBy` from the Project IRI to the new Bullet IRI(s), delete the
      Project note.
- [ ] Check `rg:deliveredDuring`'s remaining role once these Projects are
      gone — decide whether it moves to Bullet or is dropped for these nine.
- [ ] Fold in the Verizon CMDB/Neo4j-early-adopter item (`Software
      Engineering Supervisor — Verizon.md`) as a new bullet — same pattern,
      it was never a project note to begin with.
- [ ] Corporate Action Workflow Management's `startDate` question resolves
      as a side effect: bullets don't carry dates, so the duplicate-date
      concern disappears once it's a bullet, not a Project.
- [ ] Commit split: schema commit (WS2, if not already landed) separate from
      this data commit (9 deletions + N new bullets + evidence repoints).

### WS6 — Mechanism: `graph_html.py` refactor
**Tier: Sonnet · Depends on: none (do before WS7) · Domain: mechanism**

- [ ] Extract `_TEMPLATE` (`graph_html.py:185`, inline HTML) into a separate
      static template file loaded at build time. Extract `pdf.py`'s `_CSS`
      block (line 179) into a real `.css` file the same way — WeasyPrint's
      `CSS(filename=...)` takes a path.
- [ ] Remove the upper-left cloud-cluster filter box.
- [ ] Redesign the type-filter as a left sidebar (still just node-type
      toggles at this stage — the richer skill browser is WS7).
- [ ] **Org-flooding fix, root cause found**: it's not a query-layer problem
      — `resume_model.py`'s position/cert/education queries already keep
      `rg:organization` (employer) and `sdo:recognizedBy` (credentialing
      body) separate. It's a *rendering* artifact: `graph_html.py`'s
      `TYPE_META` has one `Organization` bucket regardless of which edge
      brought the node in, so EXIN/Scaled Agile render identically to real
      employers and clutter that visual cluster. Fix at the render layer:
      either split into two node types (Employer / CredentialingBody) or
      exclude `recognizedBy` targets from the node set and keep them as a
      text property on the cert's side panel instead.
- [ ] Skill-level bar chart: in the node side panel, render `rg:level` as an
      increasing bar (aware → expert, left to right) instead of/alongside
      plain text.

### WS7 — Mechanism: date-axis + skill-recency sidebar
**Tier: Opus · Depends on: WS6, WS4/WS5 (evidence chains must be final) ·
Domain: mechanism**

- [ ] Per-skill "last exercised" derivation: skill → `evidencedBy` →
      Bullet/Project → owning Position's dates (or Project's own dates) —
      must wait for WS5 since evidence now sometimes routes through a
      Bullet instead of directly through a Position/Project.
- [ ] Simple date-range quick-filter (e.g. "last 10 years") — do this now,
      it's cheap and self-contained.
- [ ] The VS-Code-style left bar (skill list sorted by last-exercised desc,
      multi-select, cascading filter into evidence nodes) — **recommendation:
      defer the cascading multi-select to the M7/M8 Neo4j+NVL page.**
      Cascading graph filtering is exactly what Cypher neighbor-expansion
      queries do cheaply; hand-rolling equivalent traversal in the vanilla
      force-layout canvas now is throwaway work once M7/M8 lands. Ship the
      quick date filter here; park the rich browser as an M7/M8 NVL feature.

### WS8 — Docs: minted-IRI procedure
**Tier: Haiku · Depends on: none · Domain: docs**

- [ ] Document how `vault_to_rdf.py` mints IRIs (`pipeline/vendor/
      vault_to_rdf.py:510-526`): base (from the nearest governing
      `context.jsonld`, walking up from the note) + percent-encoded file
      stem. **Folders never enter the IRI** — two notes with the same
      filename in different folders mint the *same* IRI and collide (this
      already happened once — the `Data Engineering Zoomcamp Coursework`
      project was deliberately renamed to avoid colliding with the `Data
      Engineering Zoomcamp` cert note). Write this collision rule up
      explicitly — it's a live authoring trap, not just trivia.

### WS9 — Docs: architecture sequence diagram
**Tier: Sonnet (use the Lucid tool) · Depends on: none · Domain: docs**

- [ ] Sequence diagram: `vault/` → `vault_to_rdf.py` → `build.py` →
      `validate.py` (pySHACL gate, fail = CI failure) → `project.py`
      (`--extra-graph`/`--application` branch) → exports (`json_resume.py`
      / `pdf.py` / `graph_html.py`) → `dist/`. Name real functions/classes
      from those files, not generic boxes.

### WS10 — [private repo] AstraZeneca application summary
**Tier: Opus · Run in `resume-applications`, not this repo**

- [ ] `vault/applications/AstraZeneca — Knowledge Graph and AI Engineer.md`
      summary rewrite. **Must not be actioned here** — this repo's own rule
      bars employer names; open a session directly in `resume-applications`
      where that context lives.

---

## Suggested order

Parallelizable immediately: **WS1, WS2, WS8, WS9** (no dependencies).
Then: **WS3, WS4** (need WS2). Then **WS5** (needs WS2, is the biggest single
session — don't split it further, the cross-file consistency is the point).
Then **WS6 → WS7** (sequential, WS7 also wants WS4/WS5 settled). **WS10**
anytime, separate repo.
