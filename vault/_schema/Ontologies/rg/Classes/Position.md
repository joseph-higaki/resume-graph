---
type: owl:Class
label: Position
comment: "An employment role held at an organization, with dates."
subClassOf: [ sdo:EmployeeRole ]
---
# Position

One note per role in `vault/positions/`. Anchored to `sdo:EmployeeRole` (a
qualified relation between person and organization) — M7 collapses these into
`HELD_ROLE` relationships in the LPG remodel.
